To run the code print these into the terminal: 

npm create vite@latest Wiki-finder-ui --template react-ts
cd Wiki-finder-ui
npm install
npm run dev


* You have to choose the framework react and the variant typecript when prompted to
* After creating the folder you have to go into src/app.tsx and copy over the code from the file I uploaded (If not already done)

When you want to rerun the program:

cd Wiki-finder-ui
npm run dev


* The Code for the propgram is under src/App.tsx